name: example-check
description: "示例：验证目标页面加载正常"
target_url: "{base_url}"
goal: "打开目标页面，验证页面正常加载，检查页面标题不为空，页面上有可见内容"
assertions:
  - "页面正常加载，无错误信息"
  - "页面标题不为空"
max_steps: 15
timeout_seconds: 60
test_data: {}
